.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# Experiment output and trained models are reproducible from the CLI.
ExpOutput/
Models/
*.h5
*.keras
*.weights.h5

.vscode/
.idea/
.DS_Store

# Personal drafts, not part of the project.
SHARE.md

# Large external datasets, fetched by scripts rather than tracked.
Data/depmap/
Data/variants/
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/main.pdf
Data/causalchamber/
Data/circadian/
Data/celegans/
Data/zapbench/
Data/eeg/
Data/climate/

# ---------------------------------------------------------------------------
# Manuscript sources and the arXiv submission package.
# The paper is distributed through arXiv, not this repository. What the paper
# promises to release lives here and stays tracked: scripts/, the experiment
# ledger (paper/causal_detection_log.md) and the pre-registrations
# (paper/validation_protocol.md).
paper/*.tex
paper/*.pdf
# ...but the compiled manuscript itself is tracked, so a reader arriving from
# the preprint finds the paper beside the code it describes.
!paper/excess_paper.pdf
paper/*.bib
paper/Makefile
paper/figs/
paper/arxiv/
paper/arxiv_submission.tar.gz

# Superseded drafts. These still assert claims the corrected manuscript
# withdraws (per-cell intervention resolution, the doubled CCM cost, the
# four-systems framing), so publishing them beside it would mislead.
paper/draft.md
paper/preprint_excess_detector.md
paper/preprint_mechanisms.md
paper/design_curve_discriminator.md
paper/proposal_depmap.md

# Manuscript-production tooling. These operate on the LaTeX, which is not
# distributed here; the analysis and verification scripts beside them are.
scripts/make_arxiv_package.py
scripts/render_float_pages.py
scripts/check_float_drift.py
scripts/check_float_refs.py
scripts/diagnose_render.py
scripts/check_citations.py

# Windows shell redirect artifacts.
$null
NUL

# Study data fetched by scripts; large and redistributable from source.
Data/wormwideweb/
Data/depmap_24q4/
Data/ieeg876/

# Submission logistics and derived text; the canonical artifacts are the
# manuscript on the preprint server and the protocols tracked here.
paper/biorxiv/
paper/abstract_plain.txt
paper/excess_paper.tex
paper/depmap_paper.tex
paper/tmlr/
paper/depmap_plos.tex
paper/depmap_plos.pdf
paper/plos_cover_letter.md
paper/depmap_paper.pdf
